{
  "_comment": "Motors HTTP API (CRUD + cache + interest). Keep this off :9090 so it does not collide with observability.",
  "http_addr": ":8081",
  "price_cache_ttl_sec": 60,
  "vpq_debug": false
}
